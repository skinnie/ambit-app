# Tools format spec

The Ambit3 navigation-database format spec and full module-by-module reference live in
[`tools/README.md`](https://github.com/skinnie/ambit-app/blob/main/tools/README.md) at the
repo root (kept there rather than duplicated here, since it sits next to the code it describes).

For everyday usage commands (decoding, building, writing to a watch), see
[Use the tools](../how-to/using-the-tools.md).
